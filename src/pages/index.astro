---
import MainLayout from '../layouts/MainLayout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
---

<MainLayout title="Altruismo Eficaz UPY">
  <Navigation />
  
  <main class="hero-section">
    <div class="container">
      <div class="content-wrapper">
        <div class="text-content">
          <h1>Altruismo Eficaz <br>UPY</h1>
          <p>Impacto positivo basado en evidencia y razón.</p>
        </div>
        
        <div class="image-container">
          <img src="/Foto_EA_UPY.jpg" alt="Altruismo Eficaz UPY" />
        </div>
      </div>
    </div>
  </main>

  <div class="spacer"></div>

  <section class="info-section">
    <div class="container">
      <div class="info-card">
        <div class="info-content">
          <h2>¿Qué es Altruismo Eficaz?</h2>
          <p>Altruismo Eficaz es un movimiento global que utiliza evidencia y razonamiento para determinar las formas más efectivas de mejorar el mundo. En EA UPY construimos una comunidad de estudiantes comprometidos con causar el mayor impacto positivo posible.</p>
          
          <a href="/about" class="text-link">Conoce más sobre nosotros <span class="arrow">→</span></a>
        </div>
        
        <div class="info-boxes">
          <div class="info-box">
            <h3>Proyectos</h3>
            <p>Descubre nuestras iniciativas actuales</p>
            <a href="/projects" class="box-link">→</a>
          </div>
          
          <div class="info-box">
            <h3>Eventos</h3>
            <p>Participa en nuestras actividades</p>
            <a href="/events" class="box-link">→</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</MainLayout>

<style>
  .hero-section {
    min-height: 80vh;
    padding: 5% 2%;
    position: relative;
  }

  .spacer {
    height: 10vh; /* Espacio adicional entre secciones */
    width: 100%;
  }

  .container {
    width: 90%;
    max-width: 1300px;
    margin: 0 auto;
  }
  
  .content-wrapper {
    display: flex;
    align-items: center;
    gap: 5%;
    width: 100%;
  }
  
  .text-content {
    flex: 1;
    text-align: left;
  }

  h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  p {
    font-size: clamp(1rem, 1.2vw, 1.1rem);
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
    max-width: 600px;
  }
  
  .image-container {
    flex: 1;
  }

  .image-container img {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.5);
  }

  /* Estilos para la sección de información */
  .info-section {
    padding: 0 2% 5%;
    width: 100%;
    position: relative;
  }

  .info-card {
    background-color: rgba(139, 107, 255, 0.08);
    border-radius: 16px;
    padding: 5%;
    display: flex;
    gap: 5%;
    width: 100%;
    max-width: 100%;
    margin: 0 auto;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
  }

  .info-content {
    flex: 2;
  }

  .info-content h2 {
    font-size: 2rem;
    margin-bottom: 5%;
  }

  .info-content p {
    font-size: 1.1rem;
    line-height: 1.6;
    margin-bottom: 5%;
    color: var(--color-text);
    opacity: 0.9;
  }

  .text-link {
    display: inline-flex;
    align-items: center;
    color: var(--color-accent);
    font-weight: 500;
    gap: 0.5rem;
    transition: all 0.3s ease;
  }

  .text-link:hover {
    text-decoration: none;
    color: var(--color-accent-hover);
  }

  .arrow {
    transition: transform 0.3s ease;
  }

  .text-link:hover .arrow {
    transform: translateX(5px);
  }

  .info-boxes {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 8%;
  }

  .info-box {
    background-color: rgba(139, 107, 255, 0.15);
    border-radius: 12px;
    padding: 8%;
    position: relative;
    transition: all 0.3s ease;
  }

  .info-box:hover {
    background-color: rgba(139, 107, 255, 0.25);
  }

  .info-box h3 {
    font-size: 1.2rem;
    margin-bottom: 3%;
  }

  .info-box p {
    font-size: 0.9rem;
    opacity: 0.8;
    margin-bottom: 0;
  }

  .box-link {
    position: absolute;
    bottom: 8%;
    right: 8%;
    color: var(--color-accent);
    font-size: 1.2rem;
    transition: transform 0.3s ease;
  }

  .info-box:hover .box-link {
    transform: translateX(5px);
  }

  @media (max-width: 968px) {
    .content-wrapper {
      flex-direction: column;
    }
    
    .text-content {
      text-align: center;
    }

    .info-card {
      flex-direction: column;
      gap: 6%;
      padding: 6%;
    }
    
    .spacer {
      height: 5vh;
    }
  }

  @media (max-width: 768px) {
    .hero-section {
      min-height: auto;
      padding: 10% 4%;
    }

    .spacer {
      height: 3vh;
    }

    .info-section {
      padding: 0 2% 10%;
    }
    
    .image-container img {
      width: 90%;
      margin: 0 auto;
      display: block;
    }
    
    h1 {
      font-size: clamp(2rem, 4vw, 3rem);
    }
    
    p {
      font-size: 0.95rem;
    }
    
    .info-content h2 {
      font-size: 1.75rem;
    }
  }
</style>